import { Box, Typography } from '@mui/material';
import React from 'react';

const Cart = () => {
    return (
        <Box >
            <Typography variant="h3">
                Cart
            </Typography>;

        </Box>
    );
};

export default Cart;